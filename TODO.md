
# m2b
- Instead of using window.prompt (which doesn't work well when viewing a video in full screen), let's create a custom text modal when "s" is pressed, and then either `enter` to continue or `esc` to cancel.

# m2a

- Switch the shortcuts from cmd+v and v to s and d, respectively, and update the docs.